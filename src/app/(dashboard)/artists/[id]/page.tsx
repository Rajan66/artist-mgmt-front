import React from "react";

import { ArtistEditForm } from "@/features/artists/components";

const page = () => {
  return (
    <div>
      <ArtistEditForm />
    </div>
  );
};

export default page;
